const upperCase = (string) => string.toUpperCase();

const firstLetter = (string) => string.charAt(0);

const stringConcater = (string1, string2) => string1 + string2;

module.exports = { upperCase, firstLetter, stringConcater };
